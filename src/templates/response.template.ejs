import { definitions } from '<%= definitionsRelPath.replace(/(^\.\.\/)/, './').replace(/\.ts$/, '') %>';


export namespace ISwaggerResponse {
<% if (definitions && definitions.length) { %>
<%  for (let i = 0, length = definitions.length; i < length; i++) { %>
  export type <%= definitions[i].replace(/<|>/g, '') %> = definitions['<%- definitions[i] %>'];
<%  } %>
<% } %>
}
